[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application fallback for TanStack Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

[[headers]]
  for = "/*"
    [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"